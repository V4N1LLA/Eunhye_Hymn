# 유스케이스: Ping 헬스 체크

## 목표
- API 가용성을 빠르게 확인한다.

## 주요 사용자
- API 클라이언트(관리자 웹/모바일).

## 사전 조건
- API 서비스가 실행 중이다.

## 정상 흐름
1. 클라이언트가 `GET /ping` 요청을 보낸다.
2. API는 `{ "ok": true }` 데이터를 성공 엔벨로프로 반환한다.

## 예외 흐름
- 서버 장애 시 네트워크 오류가 반환된다.

## 관련 데이터
- 없음.

## API 참조
- `GET /ping`
