# 유스케이스: 이벤트 기록

## 목표
- 사용자 행동 이벤트를 수집해 분석/감사에 활용한다.

## 접근 조건
- 인증 필요.

## 처리 흐름
1. 요청 본문이 단건인지 배열인지 판별한다.
2. `eventType`을 enum으로 검증한다.
3. 이벤트를 저장한다.

## 실패 케이스
- `eventType`이 유효하지 않으면 `400 Bad Request`.
- `part` 값이 유효하지 않으면 `400 Bad Request`.
