# ==============================================================
# Eunhye Hymn — Docker Compose 환경변수 템플릿
# ==============================================================
# 사용법: 이 파일을 .env로 복사하여 사용하세요.
#   cp .env.example .env
#
# docker-compose는 자동으로 .env 파일을 읽습니다.
# ==============================================================

# ── PostgreSQL ───────────────────────────────────────────────
POSTGRES_DB=eunhye_hymn
DB_USER=postgres
DB_PASS=postgres

# ── JWT (필수 — 미설정 시 API 부팅 실패) ────────────────────
JWT_SECRET=dev-secret-key-change-in-production-min-32-chars!!
JWT_ACCESS_TTL_SECONDS=3600
JWT_REFRESH_TTL_SECONDS=604800

# ── 초대코드 (필수 — 미설정 시 API 부팅 실패) ───────────────
INVITE_CODE=dev-invite-code

# ── S3 / LocalStack ─────────────────────────────────────────
S3_BUCKET=local-bucket
S3_REGION=ap-northeast-2
S3_ENDPOINT=http://localstack:4566
S3_PUBLIC_BASE_URL=http://localhost:4566/local-bucket
S3_PRESIGN_EXPIRES_MINUTES=15

# ── Spring Profile ───────────────────────────────────────────
SPRING_PROFILES_ACTIVE=dev
